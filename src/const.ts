export enum AppRoute {
  Catalog = '/catalog',
  Root = '/'
}

export enum APIRoute {
  Cameras = '/cameras',
  Promo = '/promo',
  Reviews = '/reviews',
  Coupons = '/coupons',
  Orders = '/orders'
}

export enum NameSpace {
  Main = 'MAIN',
}
